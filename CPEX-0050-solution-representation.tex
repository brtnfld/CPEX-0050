\documentclass[11pt,letterpaper]{article}

%% ── Packages ──────────────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{longtable}
\usepackage{array}
\usepackage{colortbl}
\usepackage{parskip}
\usepackage{microtype}
\usepackage{hyphenat}
\setlength{\emergencystretch}{3em}
\usepackage{needspace}
\usepackage[right,running]{lineno}

%% ── Suppress lineno inside code listings ────────────────────────────────
\makeatletter
\let\cgns@orig@lstlisting\lstlisting
\let\cgns@endorig@lstlisting\endlstlisting
\renewenvironment{lstlisting}
  {\par\needspace{5\baselineskip}\nolinenumbers\cgns@orig@lstlisting}
  {\cgns@endorig@lstlisting\par\linenumbers}
\makeatother

%% ── Color palette ───────────────────────────────────────────────────────
\definecolor{cgnsblue}{HTML}{1F4E79}
\definecolor{cgnsteal}{HTML}{2E7D8C}
\definecolor{cgnslight}{HTML}{D6EAF0}
\definecolor{cgnsgold}{HTML}{C78C1C}

%% ── Hyperlink style ──────────────────────────────────────────────────────
\hypersetup{
  colorlinks=true,
  linkcolor=cgnsblue,
  urlcolor=cgnsteal,
  citecolor=cgnsblue,
}

%% ── Code listing style ──────────────────────────────────────────────────
\lstset{
  basicstyle=\scriptsize\ttfamily,
  keywordstyle=\bfseries\color{cgnsblue},
  commentstyle=\itshape\color{cgnsteal},
  stringstyle=\color{red!60!black},
  breaklines=true,
  frame=single,
  framerule=0.5pt,
  rulecolor=\color{cgnsblue!40},
  backgroundcolor=\color{cgnslight!30},
  numbers=left,
  numberstyle=\tiny\color{cgnsblue!50},
  numbersep=6pt,
  showstringspaces=false,
  xleftmargin=1em,
  xrightmargin=1em,
  aboveskip=1em,
  belowskip=1em,
  tabsize=4,
}

%% ── SIDS-style node definition environment ──────────────────────────────
\newenvironment{sidsnode}[1]{%
  \par\vspace{0.8em}%
  \noindent\colorbox{cgnslight!50}{\makebox[\dimexpr\linewidth-2\fboxsep][l]{%
    \texttt{\textbf{\color{cgnsblue}#1}}}}\\[0.3em]%
  \begin{quote}\ttfamily
}{%
  \end{quote}\vspace{0.4em}%
}

%% ── Header / Footer ────────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\color{cgnsblue} CPEX 0050 --- Solution Continuity Type}
\fancyhead[R]{\small\color{cgnsblue} Version 1.0}
\fancyfoot[C]{\small\color{cgnsblue}\thepage}
\renewcommand{\headrulewidth}{0.5pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{cgnsgold}\leaders\hrule height\headrulewidth\hfill}}
\renewcommand{\footrulewidth}{0pt}

%% ── Title Formatting ───────────────────────────────────────────────────
\titleformat{\section}{\Large\bfseries\color{cgnsblue}}{\thesection}{0.6em}{}[\vspace{0.2em}{\color{cgnsgold}\titlerule[0.8pt]}]
\titleformat{\subsection}{\large\bfseries\color{cgnsblue!85}}{\thesubsection}{0.6em}{}
\titleformat{\subsubsection}{\normalsize\bfseries\color{cgnsteal}}{\thesubsubsection}{0.6em}{}

%% =====================================================================
\begin{document}
\linenumbers

%% ── Title Block ─────────────────────────────────────────────────────────
\begin{center}
  \includegraphics[width=0.25\textwidth]{CGNS_logo_1.png}\\[1.2em]
  {\LARGE\bfseries\color{cgnsblue} CGNS Proposal for Extension 0050}\\[0.6em]
  {\Large\color{cgnsteal} Solution Continuity Type}\\[0.4em]
  {\color{cgnsgold}\rule{0.4\textwidth}{0.8pt}}\\[0.6em]
  {\large Version 1.0}\\[0.6em]
  \today
\end{center}

\vspace{1em}

%% ── Metadata Table ──────────────────────────────────────────────────────
\noindent
\rowcolors{2}{cgnslight!30}{white}
\begin{tabular}{@{} >{\bfseries\color{cgnsblue}}l l @{}}
\toprule
CPEX\#           & 0050 \\
Scope            & Distinguish continuous and discontinuous solution continuity \\
Champion         & M.\ Scot Breitenfeld \\
Organization     & The HDF Group \\
E-mail           & \href{mailto:brtnfld@hdfgroup.org}{brtnfld@hdfgroup.org} \\
GitHub Issue     & TBD \\
Reference Impl.  & TBD \\
Target Release   & TBD \\
Date First Posted & \today \\
SIDS Status      & proposed \\
Filemap Status   & proposed \\
MLL Status       & proposed \\
\bottomrule
\end{tabular}

\vspace{1.5em}

\tableofcontents
\newpage

%% ═════════════════════════════════════════════════════════════════════════
\section{Abstract}
%% ═════════════════════════════════════════════════════════════════════════

This CPEX proposes adding a \texttt{SolutionContinuity\_t}
enumeration and corresponding SIDS node to the CGNS standard, enabling
CGNS files to record whether solution data uses a continuous or
discontinuous field continuity.  This distinction---most commonly
associated with Continuous Galerkin (CG) and Discontinuous Galerkin
(DG) finite element methods---is fundamental to the correct
interpretation of solution data but is not captured by any existing
CGNS construct.  The proposal is general: it applies at any polynomial
order, to any discretization family (finite element, finite volume,
spectral element, etc.), and interacts naturally with the higher-order
solution infrastructure introduced by CPEX~0045.

%% ═════════════════════════════════════════════════════════════════════════
\section{Motivation and Rationale}
%% ═════════════════════════════════════════════════════════════════════════

\subsection{Problem Statement}

Modern CFD and computational mechanics codes increasingly use
discretization methods whose solution data cannot be fully described
by the existing CGNS metadata.  The core issue is the treatment of
degrees of freedom (DOFs) at shared geometric entities (vertices,
edges, faces):

\begin{itemize}[leftmargin=2em]
\item In a \textbf{continuous} representation (e.g., Continuous
  Galerkin FEM, $C^0$ spectral element methods), DOFs at shared
  interfaces are \emph{shared}: a single value exists per geometric
  node, and the global solution field is at least $C^0$-continuous
  across element boundaries.

\item In a \textbf{discontinuous} representation (e.g., Discontinuous
  Galerkin, finite volume, $C^{-1}$ spectral element methods), DOFs
  are \emph{element-local}: each element owns its own values at every
  point, including at shared interfaces.  Multiple independent values
  may exist at the same geometric location.
\end{itemize}

This distinction affects:

\begin{enumerate}[leftmargin=2em]
\item \textbf{Data size and layout.}  A DG solution on a mesh with
  shared vertices stores more values than a CG solution on the same
  mesh.  Post-processors that assume shared-vertex semantics will
  misinterpret the data dimensions.

\item \textbf{Visualization and interpolation.}  CG fields can be
  interpolated globally using the mesh connectivity; DG fields require
  element-local interpolation and may exhibit intentional
  discontinuities at element interfaces that should not be smoothed.

\item \textbf{Restart and data exchange.}  Solvers reading restart
  files must know whether DOFs are shared or element-local to
  correctly reconstruct the solution state.  Coupling frameworks
  exchanging solution data between codes need this metadata to perform
  correct inter-code transfers.  In Fluid-Structure Interaction (FSI)
  applications, for example, a continuous FEM structural displacement
  field and a discontinuous DG fluid field coexist---the coupling
  framework must use different interpolation and projection operators
  for each, and the continuity metadata enables this dispatch.

\item \textbf{Error estimation and adaptation.}  The jump in solution
  values across element interfaces in a DG solution is physically
  meaningful and is used for error estimation.  A post-processor that
  incorrectly treats DG data as continuous would average out these
  jumps.
\end{enumerate}

\subsection{Inadequacy of Existing CGNS Constructs}

No existing CGNS mechanism captures the continuous/discontinuous
distinction:

\begin{description}[leftmargin=2em]
\item[\texttt{GridLocation\_t}] specifies \emph{where} data is
  located (vertices, cell centers, faces, edges, or interpolation
  points) but not whether values at shared locations are shared or
  element-local.  A \texttt{Vertex}-located solution could be either
  CG or DG; the same applies to \texttt{InterpolationPoints}.

\item[\texttt{PointSet}] (\texttt{PointList}, \texttt{PointRange})
  can index subsets of data but does not encode continuity semantics.

\item[\texttt{InterpolationType\_t}] (CPEX~0045) describes the
  polynomial basis (e.g., Lagrange, monomial) but not whether
  that basis yields a globally continuous or element-local field.

\item[\texttt{SolutionInterpolation\_t}] (CPEX~0045) records
  polynomial order and basis type per element type but contains no
  continuity information.

\item[Data array size] can sometimes be used to \emph{infer} the
  representation---a DG field will have $N_{\mathrm{elem}} \times
  N_{\mathrm{DOF/elem}}$ values versus $N_{\mathrm{nodes}}$ for CG.
  However, this inference is fragile, ambiguous for $p=0$
  (cell-centered) data, and breaks entirely for mixed-element meshes
  or when point sets are used.
\end{description}

\subsection{Generality Beyond Higher-Order Methods}

Although often discussed in the context of high-order finite elements,
the continuous/discontinuous distinction is \textbf{not limited to
higher-order methods}:

\begin{itemize}[leftmargin=2em]
\item \textbf{DG $p=0$} (piecewise constant) is essentially a
  cell-centered finite volume representation.
\item \textbf{DG $p=1$} (piecewise linear, discontinuous) is widely
  used and is a low-order method.
\item \textbf{CG $p=1$} is standard continuous linear FEM, the most
  common finite element discretization.
\item Classical \textbf{finite volume} representations are inherently discontinuous ($p=0$ DG).
\item \textbf{Hybridizable DG (HDG)} methods have both element-local
  DOFs (discontinuous) and trace DOFs on faces (continuous on the
  skeleton).
\item \textbf{FSI and multi-physics coupling} often combines a
  continuous FEM structural solver (shared displacement DOFs) with a
  discontinuous DG or FV fluid solver (element-local flow variables)
  within the same file or across coupled zones.
\end{itemize}

A proper metadata field should therefore live at the
\texttt{FlowSolution\_t} level, applicable at any order, not solely
within the higher-order interpolation infrastructure.

%% ═════════════════════════════════════════════════════════════════════════
\section{Detailed Description}
%% ═════════════════════════════════════════════════════════════════════════

\subsection{Design Overview}

The proposal introduces:

\begin{enumerate}[leftmargin=2em]
\item A new \texttt{SolutionContinuity\_t} enumeration with values
  covering the common continuity types.
\item A new optional child node of \texttt{FlowSolution\_t},
  \texttt{DiscreteData\_t}, and \texttt{ZoneSubRegion\_t} recording
  the solution continuity.
\item Mid-Level Library functions to read and write the continuity.
\item Fortran bindings.
\end{enumerate}

\subsection{New Enumeration: \texttt{SolutionContinuity\_t}}
\label{sec:enum}

\begin{lstlisting}[language=C]
typedef enum {
  CGNS_ENUMV( SolutionContinuityNull )        = CG_Null,
  CGNS_ENUMV( SolutionContinuityUserDefined )  = CG_UserDefined,
  CGNS_ENUMV( SolutionContinuous )                  = 2,
  CGNS_ENUMV( SolutionDiscontinuous )               = 3
} CGNS_ENUMT( SolutionContinuity_t );

#define NofValidSolutionContinuity 4

extern CGNSDLL const char *
    SolutionContinuityName[NofValidSolutionContinuity];

CGNSDLL const char *cg_SolutionContinuityName(
    CGNS_ENUMT(SolutionContinuity_t) type);
\end{lstlisting}

The enumeration values are:

\begin{description}[leftmargin=2em]
\item[\texttt{SolutionContinuityNull}] No continuity
  specified.  This is the default for backward compatibility: existing
  files and codes that do not set this field are unaffected.

\item[\texttt{SolutionContinuityUserDefined}] Application-defined
  continuity not covered by the standard values.  When this value is
  used, writers \emph{should} attach a \texttt{Descriptor\_t} child
  node to the \texttt{FlowSolution\_t} (or
  \texttt{DiscreteData\_t}/\texttt{ZoneSubRegion\_t}) named
  \texttt{"SolutionContinuityDescription"} whose string value
  identifies the continuity convention (e.g.,
  \texttt{"HDG-trace"}, \texttt{"C1-spline"}).  This allows readers
  to recognise the convention without requiring a separate
  side-channel.

\item[\texttt{SolutionContinuous}] DOFs at shared mesh entities
  (vertices, edges, faces) are \emph{shared} between adjacent
  elements: each shared entity carries exactly one value.  This is
  a statement about \emph{storage and DOF-sharing semantics}, not
  about the mathematical smoothness of the field.
  Consequently, $H(\mathrm{div})$-conforming spaces
  (e.g., Raviart--Thomas) and $H(\mathrm{curl})$-conforming spaces
  (e.g., N\'{e}d\'{e}lec) should also use this label: their DOFs are
  shared at element interfaces (normal-flux or tangential-component
  DOFs respectively), even though the full vector field is not
  pointwise $C^0$-continuous.  Typical of: Continuous Galerkin FEM,
  $C^0$ spectral element methods, isogeometric analysis with
  $C^0$~junctions, $H(\mathrm{div})$ and $H(\mathrm{curl})$
  conforming spaces.

\item[\texttt{SolutionDiscontinuous}] The solution field is
  element-local: each element owns independent DOFs, including at
  shared geometric locations, and no interface DOF is shared between
  adjacent elements.  Typical of: Discontinuous Galerkin methods,
  finite volume methods, $C^{-1}$ spectral element methods.
\end{description}

\subsubsection{Why Two Values Are Sufficient}

The binary continuous/discontinuous classification (enum values
\texttt{SolutionContinuous} and \texttt{SolutionDiscontinuous}) is
deliberate.  Methods that appear to require finer distinctions are
already handled by the existing two values combined with standard
CGNS patterns:

\begin{description}[leftmargin=2em]
\item[HDG and mixed-continuity methods] use both element-local
  volume DOFs and globally continuous trace DOFs.  These are stored as
  separate \texttt{FlowSolution\_t} nodes---one marked
  \texttt{SolutionDiscontinuous}, the other
  \texttt{SolutionContinuous}---following the same pattern CGNS already
  uses to separate fields at different \texttt{GridLocation\_t} values.
  A dedicated \texttt{MixedContinuity} value would conflate two distinct
  data layouts into a single node, making it harder, not easier, for
  post-processors to interpret the data.

\item[Penalized / weakly-continuous DG methods] enforce continuity
  through penalty terms in the variational formulation, but the data
  layout remains element-local: each element owns its own DOFs.  The
  correct label is \texttt{SolutionDiscontinuous}.  ``Weak continuity''
  is a solver-internal property of the numerical method, not a property
  of the stored data that readers and post-processors need to act on.

\item[Isogeometric analysis ($C^k$, $k \geq 1$)] uses basis functions
  with higher-than-$C^0$ smoothness, but the data layout is still
  globally continuous with shared DOFs at patch interfaces.  The
  correct label is \texttt{SolutionContinuous}.  The smoothness order
  $k$ is a property of the basis, not of the data layout; it belongs in
  the interpolation metadata (CPEX~0045), not in the continuity type.
\end{description}

The \texttt{SolutionContinuityUserDefined} value remains available
as an escape hatch, consistent with CGNS convention for all
enumeration types.

\textbf{Scope note.}  \texttt{SolutionContinuity\_t} captures
\emph{storage and DOF-sharing semantics} only---it answers
``are interface DOFs shared or element-local?''  It does not
encode the polynomial basis, smoothness order, patch topology,
or support-radius metadata that IGA, meshless, or other
non-standard discretizations may additionally require.  Those
richer descriptions belong in \texttt{SolutionInterpolation\_t}
(CPEX~0045) or in future extensions; this CPEX is intentionally
scoped to the coarser, universally applicable
continuous/discontinuous distinction.

\subsection{SIDS Node Definition}
\label{sec:sids-node}

The continuity is stored as a new optional child node of
\texttt{FlowSolution\_t}.  The amended SIDS definition adds one line
(shown with the \texttt{+} marker).  The definition below includes the
\texttt{SolutionInterpolation\_t} child introduced by CPEX~0045:

\begin{sidsnode}{FlowSolution\_t (amended, SIDS Section 7.7)}
FlowSolution\_t< int CellDimension, int IndexDimension, \\
\qquad\qquad\quad int VertexSize[IndexDimension], \\
\qquad\qquad\quad int CellSize[IndexDimension] > := \\
\quad \{ \\
\quad List( Descriptor\_t Descriptor1 ... DescriptorN ) ;  \qquad\qquad (o) \\
\\
\quad GridLocation\_t GridLocation ; \qquad\qquad\qquad\qquad\qquad\quad\; (o/d) \\
\\
\quad Rind\_t<IndexDimension> Rind ; \qquad\qquad\qquad\qquad\qquad\quad (o/d) \\
\\
\quad IndexRange\_t<IndexDimension> PointRange ; \qquad\qquad\qquad\;\; (o) \\
\quad IndexArray\_t<IndexDimension, ListLength[], int> PointList ; \; (o) \\
\\
\quad List( DataArray\_t<DataType, IndexDimension, DataSize[]> \\
\qquad\quad DataArray1 ... DataArrayN ) ; \qquad\qquad\qquad\qquad\quad\;\; (o) \\
\\
\quad DataClass\_t DataClass ; \qquad\qquad\qquad\qquad\qquad\qquad\quad\; (o) \\
\\
\quad DimensionalUnits\_t DimensionalUnits ; \qquad\qquad\qquad\quad\;\;\; (o) \\
\\
\quad SolutionInterpolation\_t SolutionInterpolation ; \qquad\quad\;\;\;\; (o) \quad\textit{(CPEX 0045)} \\
\\
+ \quad \textcolor{red}{SolutionContinuity\_t SolutionContinuity} ; \qquad\quad\;\; (o) \\
\\
\quad List( UserDefinedData\_t UserDefinedData1 ... UserDefinedDataN ) ; (o) \\
\quad \}
\end{sidsnode}

The \texttt{SolutionContinuity} child is \textbf{optional}
(\texttt{o}).  When absent, the continuity is unspecified
(\texttt{SolutionContinuityNull}), preserving full backward
compatibility.

The same optional child node is added to \texttt{DiscreteData\_t}
(SIDS Section~12.4), using identical structure and semantics:

\begin{sidsnode}{DiscreteData\_t (amended, SIDS Section 12.4)}
DiscreteData\_t< int CellDimension, int IndexDimension, \\
\qquad\qquad\quad int VertexSize[IndexDimension], \\
\qquad\qquad\quad int CellSize[IndexDimension] > := \\
\quad \{ \\
\quad ... (existing children unchanged) ... \\
\\
\quad + \quad \textcolor{red}{SolutionContinuity\_t SolutionContinuity} ; \qquad\quad\;\; (o) \\
\quad \}
\end{sidsnode}

The optional child node is also added to \texttt{ZoneSubRegion\_t}
(SIDS Section~7.9).  \texttt{ZoneSubRegion\_t} holds
\texttt{DataArray\_t} nodes for localized solutions, boundary fluxes,
or fields on sub-regions where the continuous/discontinuous distinction
applies equally:

\begin{sidsnode}{ZoneSubRegion\_t (amended, SIDS Section 7.9)}
ZoneSubRegion\_t< int RegionCellDimension > := \\
\quad \{ \\
\quad ... (existing children unchanged) ... \\
\\
\quad + \quad \textcolor{red}{SolutionContinuity\_t SolutionContinuity} ; \qquad\quad\;\; (o) \\
\quad \}
\end{sidsnode}

\subsubsection{Node Placement Rationale}

The node is placed as a child of \texttt{FlowSolution\_t} (rather
than, for example, \texttt{Zone\_t} or
\texttt{SolutionInterpolation\_t}) because:

\begin{itemize}[leftmargin=2em]
\item The continuity is a property of the \emph{solution field},
  not of the mesh or the zone.

\item A single zone may contain multiple \texttt{FlowSolution\_t}
  nodes with different continuity---for example, an HDG solver
  storing both volume (discontinuous) and trace (continuous) solutions,
  or a multi-physics code storing a continuous displacement field
  alongside a discontinuous stress field.

\item This parallels the existing \texttt{GridLocation\_t} child of
  \texttt{FlowSolution\_t}, which is also a per-solution-node
  property.

\item Placing it within the CPEX~0045 \texttt{SolutionInterpolation\_t}
  would incorrectly limit the scope to higher-order solutions, when
  the distinction applies at any order.
\end{itemize}

\subsection{New MLL Functions}
\label{sec:mll}

\subsubsection{C API}

\begin{lstlisting}[language=C]
/*
 * Write the solution continuity for a FlowSolution_t node.
 *
 * fn   - file number
 * B    - base index
 * Z    - zone index
 * S    - solution index
 * type - SolutionContinuity_t value
 */
int cg_sol_continuity_write(
    int fn, int B, int Z, int S,
    CGNS_ENUMT(SolutionContinuity_t) type);

/*
 * Read the solution continuity for a FlowSolution_t node.
 *
 * fn   - file number
 * B    - base index
 * Z    - zone index
 * S    - solution index
 * type - [out] SolutionContinuity_t value;
 *        returns SolutionContinuityNull if node absent
 */
int cg_sol_continuity_read(
    int fn, int B, int Z, int S,
    CGNS_ENUMT(SolutionContinuity_t) *type);

/*
 * Write/read the solution continuity for a DiscreteData_t node.
 * Same semantics as the FlowSolution_t counterparts.
 *
 * fn   - file number
 * B    - base index
 * Z    - zone index
 * D    - discrete data index
 * type - SolutionContinuity_t value
 */
int cg_discrete_continuity_write(
    int fn, int B, int Z, int D,
    CGNS_ENUMT(SolutionContinuity_t) type);

int cg_discrete_continuity_read(
    int fn, int B, int Z, int D,
    CGNS_ENUMT(SolutionContinuity_t) *type);

/*
 * Write/read the solution continuity for a ZoneSubRegion_t node.
 * Same semantics as the FlowSolution_t counterparts.
 *
 * fn   - file number
 * B    - base index
 * Z    - zone index
 * SR   - sub-region index
 * type - SolutionContinuity_t value
 */
int cg_subreg_continuity_write(
    int fn, int B, int Z, int SR,
    CGNS_ENUMT(SolutionContinuity_t) type);

int cg_subreg_continuity_read(
    int fn, int B, int Z, int SR,
    CGNS_ENUMT(SolutionContinuity_t) *type);

/*
 * Return the string name for a SolutionContinuity_t value.
 * Matches the convention of cg_GridLocationName, cg_DataTypeName, etc.
 * Returns NULL for out-of-range values.
 */
CGNSDLL const char *cg_SolutionContinuityName(
    CGNS_ENUMT(SolutionContinuity_t) type);
\end{lstlisting}

\textbf{Behavior:}

\begin{itemize}[leftmargin=2em]
\item The \texttt{\_write} functions create (or overwrite)
  the \texttt{SolutionContinuity} child node under the specified
  \texttt{FlowSolution\_t}, \texttt{DiscreteData\_t}, or
  \texttt{ZoneSubRegion\_t} node.  Writing
  \texttt{SolutionContinuityNull} removes the node if present.

\item The \texttt{\_read} functions return the value stored
  in the \texttt{SolutionContinuity} child node.  If the node does
  not exist (legacy file or unset), they return
  \texttt{SolutionContinuityNull} without error.
\end{itemize}

\subsubsection{Fortran API}

\begin{lstlisting}[language=Fortran]
! FlowSolution_t
subroutine cg_sol_continuity_write_f( &
    fn, B, Z, S, continuity, ier)
  integer, intent(in) :: fn, B, Z, S, continuity
  integer, intent(out) :: ier

subroutine cg_sol_continuity_read_f( &
    fn, B, Z, S, continuity, ier)
  integer, intent(in) :: fn, B, Z, S
  integer, intent(out) :: continuity, ier

! DiscreteData_t
subroutine cg_discrete_continuity_write_f( &
    fn, B, Z, D, continuity, ier)
  integer, intent(in) :: fn, B, Z, D, continuity
  integer, intent(out) :: ier

subroutine cg_discrete_continuity_read_f( &
    fn, B, Z, D, continuity, ier)
  integer, intent(in) :: fn, B, Z, D
  integer, intent(out) :: continuity, ier

! ZoneSubRegion_t
subroutine cg_subreg_continuity_write_f( &
    fn, B, Z, SR, continuity, ier)
  integer, intent(in) :: fn, B, Z, SR, continuity
  integer, intent(out) :: ier

subroutine cg_subreg_continuity_read_f( &
    fn, B, Z, SR, continuity, ier)
  integer, intent(in) :: fn, B, Z, SR
  integer, intent(out) :: continuity, ier
\end{lstlisting}

\subsection{Internal Data Structure Changes}

The \texttt{cgns\_sol} structure (\texttt{FlowSolution\_t}),
\texttt{cgns\_discrete} structure (\texttt{DiscreteData\_t}), and
\texttt{cgns\_subreg} structure (\texttt{ZoneSubRegion\_t}) in
\texttt{cgns\_header.h} each gain two fields:

\begin{lstlisting}[language=C]
typedef struct {            /* FlowSolution_t node */
    /* ... existing fields ... */

    /* CPEX 0050 */
    CGNS_ENUMT(SolutionContinuity_t) continuity;
    double continuity_id;   /* CGIO node id for overwrite/delete */
} cgns_sol;

typedef struct {            /* DiscreteData_t node */
    /* ... existing fields ... */

    /* CPEX 0050 */
    CGNS_ENUMT(SolutionContinuity_t) continuity;
    double continuity_id;
} cgns_discrete;

typedef struct {            /* ZoneSubRegion_t node */
    /* ... existing fields ... */

    /* CPEX 0050 */
    CGNS_ENUMT(SolutionContinuity_t) continuity;
    double continuity_id;
} cgns_subreg;
\end{lstlisting}

The \texttt{continuity} field defaults to
\texttt{SolutionContinuityNull} (value~0), which is the
zero-initialization value and the ``not specified'' state.  The
\texttt{continuity\_id} field stores the CGIO node identifier so
that the write function can overwrite or delete the child node; it
defaults to~0 (no node).

\subsection{Internal Lookup Table Initialization}

The string-to-enum mapping table must be initialized in
\texttt{cgns\_internals.c} so that \texttt{cgi\_SolutionContinuity()} can
resolve the C1 character data stored in CGNS files:

\begin{lstlisting}[language=C]
const char *SolutionContinuityName[NofValidSolutionContinuity] = {
    "Null",
    "UserDefined",
    "Continuous",
    "Discontinuous"
};
\end{lstlisting}

\subsection{MLL Implementation Requirements}
\label{sec:mll-impl}

\subsubsection{Internal Tree Parser Update (\texttt{cgns\_internals.c})}

When a CGNS file is opened, the MLL tree parser
crawls the HDF5/CGIO tree and populates
internal C structures.  The parser must be updated to recognize child
nodes with label \texttt{SolutionContinuity\_t} during the
traversal of \texttt{FlowSolution\_t} (\texttt{cgi\_read\_sol}),
\texttt{DiscreteData\_t} (\texttt{cgi\_read\_discrete}), and
\texttt{ZoneSubRegion\_t} (\texttt{cgi\_read\_subregion}).

The required behavior is:

\begin{enumerate}[leftmargin=2em]
\item During memory allocation of \texttt{cgns\_sol},
  \texttt{cgns\_discrete}, and \texttt{cgns\_subreg}, the
  \texttt{continuity} field is zero-initialized, which corresponds
  to \texttt{SolutionContinuityNull}.
\item During the child-node traversal, if a child with label
  \texttt{"SolutionContinuity\_t"} is found, read its C1 string
  data and map it to the enum value via
  \texttt{cgi\_SolutionContinuity(string\_data, \&continuity)}.
  Store the result in the struct's \texttt{continuity} field.
\item If no such child node exists, the field retains its default
  \texttt{SolutionContinuityNull} value.
\end{enumerate}

This follows the existing pattern used for \texttt{GridLocation\_t}
parsing in \texttt{cgi\_read\_sol}.

\subsubsection{Read Function Error Handling}

The \texttt{\_read} functions (e.g.,
\texttt{cg\_sol\_continuity\_read}) return the cached struct field
rather than performing a fresh HDF5 read.  Because the internal tree
parser already handles the absent-node case by leaving the field at
\texttt{SolutionContinuityNull}, the read function simply returns
the struct value and \texttt{CG\_OK}:

\begin{lstlisting}[language=C]
int cg_sol_continuity_read(int fn, int B, int Z, int S,
                           CGNS_ENUMT(SolutionContinuity_t) *type) {
    cgns_sol *sol;

    cg = cgi_get_file(fn);
    if (cg == 0) return CG_ERROR;

    if (cgi_check_mode(cg->filename, cg->mode, CG_MODE_READ))
        return CG_ERROR;

    sol = cgi_get_sol(cg, B, Z, S);
    if (sol == NULL) return CG_ERROR;

    *type = sol->continuity;

    return CG_OK;
}
\end{lstlisting}

This architecture avoids the need to intercept
\texttt{CG\_NODE\_NOT\_FOUND} errors at read time---the absent-node
case is handled once during file open, and all subsequent reads are
served from the in-memory cache.  Legacy code that iterates over
solutions and queries the continuity will always receive
\texttt{CG\_OK} with \texttt{SolutionContinuityNull} for files
that predate this CPEX.

\subsubsection{Write Function Implementation}

The \texttt{\_write} functions create (or overwrite) the
\texttt{SolutionContinuity} child node.  Writing
\texttt{SolutionContinuityNull} removes the node if present,
restoring the ``unspecified'' state.  The reference implementation for
\texttt{FlowSolution\_t} is shown below; the
\texttt{DiscreteData\_t} and \texttt{ZoneSubRegion\_t} variants are
analogous (substituting the appropriate internal structure accessor).

\begin{lstlisting}[language=C]
int cg_sol_continuity_write(int fn, int B, int Z, int S,
                            CGNS_ENUMT(SolutionContinuity_t) type) {
    cgns_sol *sol;
    cgsize_t length;
    double posit_id;

    cg = cgi_get_file(fn);
    if (cg == 0) return CG_ERROR;

    if (cgi_check_mode(cg->filename, cg->mode, CG_MODE_WRITE))
        return CG_ERROR;

    sol = cgi_get_sol(cg, B, Z, S);
    if (sol == NULL) return CG_ERROR;

    /* Validate enum value */
    switch (type) {
        case CGNS_ENUMV(SolutionContinuityNull):
        case CGNS_ENUMV(SolutionContinuityUserDefined):
        case CGNS_ENUMV(SolutionContinuous):
        case CGNS_ENUMV(SolutionDiscontinuous):
            break;
        default:
            cgi_error("Invalid SolutionContinuity value: %d", (int)type);
            return CG_ERROR;
    }

    /* Delete existing node if writing Null */
    if (type == CGNS_ENUMV(SolutionContinuityNull)) {
        if (sol->continuity !=
                CGNS_ENUMV(SolutionContinuityNull)) {
            if (cgi_delete_node(sol->id,
                    sol->continuity_id))
                return CG_ERROR;
            sol->continuity =
                CGNS_ENUMV(SolutionContinuityNull);
            sol->continuity_id = 0;
        }
        return CG_OK;
    }

    /* Delete existing node before overwrite */
    if (sol->continuity !=
            CGNS_ENUMV(SolutionContinuityNull)) {
        if (cgi_delete_node(sol->id,
                sol->continuity_id))
            return CG_ERROR;
        sol->continuity_id = 0;
    }

    /* Create the child node */
    length = (cgsize_t)strlen(
        SolutionContinuityName[type]);
    if (cgi_new_node(sol->id, "SolutionContinuity",
            "SolutionContinuity_t", &posit_id,
            "C1", 1, &length,
            (void *)SolutionContinuityName[type]))
        return CG_ERROR;

    sol->continuity = type;
    sol->continuity_id = posit_id;
    return CG_OK;
}
\end{lstlisting}

\textbf{Note:} The \texttt{continuity\_id} field (a \texttt{double}
storing the CGIO node identifier) must be added to the internal
structures alongside the \texttt{continuity} field so that the
node can be located for overwrite or deletion.  This follows the
pattern used by other optional child nodes in the MLL (e.g.,
\texttt{GridLocation} tracking via \texttt{location\_id} fields in
some internal paths).

%% ─────────────────────────────────────────────────────────────────────────
\subsection{Data Layout for Discontinuous Solutions at Shared Mesh Vertices}
\label{sec:shared-vertex-layout}

A common and important case arises when a DG solver uses a mesh whose
vertices are shared in the standard CGNS sense---enabling mesh walking,
stream tracing, and connectivity queries via the standard
\texttt{Elements\_t} tables---but stores element-local, independent
solution values at each element's copy of those vertices.  At a shared
vertex adjacent to $k$ elements there are $k$ distinct solution values,
one per element; readers must not average or conflate them.

The correct CGNS encoding for this pattern is
\texttt{GridLocation\_t = Vertex} combined with
\texttt{SolutionContinuity\_t = SolutionDiscontinuous} and a
\texttt{PointList} that lists every element-vertex pair explicitly
(repeated vertex indices are expected and meaningful).  The
\texttt{SolutionContinuity\_t} node is the machine-readable flag that
tells readers these repeated entries represent independent DOFs, not
duplicate data.

Without the \texttt{SolutionContinuity\_t} node, a reader cannot
distinguish this layout from a malformed continuous solution with
duplicate entries, which is why the node is necessary.  With it, a
post-processor or coupling framework can correctly:

\begin{itemize}[leftmargin=2em]
\item walk the mesh using the shared-vertex connectivity for geometry
  operations (stream tracing, surface intersection, etc.);
\item read solution values element-locally, preserving the jumps at
  shared interfaces.
\end{itemize}

For element-local DOFs at non-vertex positions (interior quadrature
points, face quadrature points, etc.), use
\texttt{GridLocation\_t = InterpolationPoints} with
\texttt{SolutionInterpolation\_t} metadata from CPEX~0045.
\texttt{GridLocation\_t = Vertex} with a \texttt{PointList} is
specifically the correct choice when the DOF positions coincide with
mesh vertices and the shared-vertex mesh connectivity is to be
preserved.

\texttt{GridLocation\_t = Vertex} with \texttt{SolutionDiscontinuous}
and \emph{no} \texttt{PointList} is not valid: the SIDS
\texttt{DataSize} formula for \texttt{Vertex} yields
$\texttt{DataSize} = \texttt{VertexSize}$ (the shared-vertex count),
which cannot represent the larger element-local DOF count.
Similarly, \texttt{PointRange} is not suitable for this case because
a contiguous range of vertex indices cannot express the element-local
repetition of shared vertices; only \texttt{PointList} provides the
necessary per-element DOF ordering.
\texttt{GridLocation\_t = CellCenter} with \texttt{SolutionDiscontinuous}
remains valid since cell-centered data is inherently one value per
element with no shared-entity ambiguity.

This approach extends naturally to mixed-element meshes
(\texttt{MIXED}, \texttt{NGON\_n}, \texttt{NFACE\_n}).  The
\texttt{PointList} simply lists vertex indices for each element in
element order; elements with different vertex counts contribute
different numbers of entries.  The \texttt{SolutionContinuity\_t =
SolutionDiscontinuous} flag tells readers that repeated vertex indices
represent independent DOFs regardless of element type heterogeneity.

\textbf{SIDS Amendment (PointList semantics under Discontinuous solutions):}
When \texttt{SolutionContinuity\_t = SolutionDiscontinuous} and a
\texttt{PointList} is present under \texttt{FlowSolution\_t} with
\texttt{GridLocation\_t = Vertex}, repeated vertex indices are
permitted and represent element-local DOFs.  The DOF-to-element
mapping is defined by traversal of the zone's \texttt{Elements\_t}
sections in ascending section index order, then by each section's
connectivity array order.  Solution array position $i$ corresponds to
the $i$-th entry of the \texttt{PointList} under this traversal.
This is an explicit amendment to the \texttt{PointList} description in
SIDS~§7.7 for this case; repeated indices under a continuous solution
remain undefined and invalid.

\textbf{Note:} For element-local DOFs at non-vertex positions, or
when the implicit element-ordering covenant above is inconvenient,
\texttt{GridLocation\_t = InterpolationPoints} with
\texttt{SolutionInterpolation\_t} from CPEX~0045 is the preferred
encoding and does not require repeated indices.

See Example~\ref{ex:shared-vertex-dg} for the complete C encoding
of this pattern.

%% ─────────────────────────────────────────────────────────────────────────
\subsection{Interaction with \texttt{Rind\_t} (Ghost Layers)}
\label{sec:rind-interaction}

\texttt{SolutionContinuity\_t} applies uniformly to the entire
solution field, including any rind (ghost) entries declared by
\texttt{Rind\_t}.  A \texttt{SolutionDiscontinuous} label means that
all data entries---interior and rind alike---are element-local: each
rind entry belongs to one element and must not be interpreted as a
shared vertex value.  DG solvers that use ghost layers for MPI halo
exchange may therefore store element-local rind DOFs in the standard
way; the \texttt{SolutionDiscontinuous} label covers them.  Rind
entries of a \texttt{SolutionContinuous} solution retain their usual
shared-DOF semantics.  No special treatment of the rind is required
beyond what \texttt{Rind\_t} already specifies; the continuity label
is consistent across the full data array.

%% ═════════════════════════════════════════════════════════════════════════
\section{Interaction with CPEX 0045 (Higher-Order Elements)}
%% ═════════════════════════════════════════════════════════════════════════

CPEX~0045 introduced \texttt{GridLocation\_t = InterpolationPoints},
\texttt{SolutionInterpolation\_t}, and the concept of per-element-type
polynomial basis definitions.  The present CPEX complements rather
than modifies that infrastructure:

\begin{itemize}[leftmargin=2em]
\item CPEX~0045 answers: ``What polynomial basis and order describes
  the solution within each element?''
\item This CPEX answers: ``Are the resulting DOFs shared across element
  interfaces or element-local?''
\end{itemize}

Both pieces of metadata are needed for a complete description of a
higher-order solution field.  They are orthogonal:

\begin{center}
\begin{tabular}{lcc}
\toprule
& \textbf{Continuous} & \textbf{Discontinuous} \\
\midrule
Lagrange $p=1$ & CG FEM & DG $p=1$ \\
Lagrange $p=3$ & CG spectral & DG spectral \\
Hierarchical $p=2$ & CG (modal) & DG (modal) \\
$p=0$ (constant) & --- & FV / DG $p=0$ \\
\bottomrule
\end{tabular}
\end{center}

For higher-order solutions using \texttt{GridLocation\_t =
InterpolationPoints}, the recommended practice is:

\begin{enumerate}[leftmargin=2em]
\item Use CPEX~0045 to define the element type, interpolation basis,
  and polynomial order via \texttt{SolutionInterpolation\_t}.
\item Use this CPEX to set \texttt{SolutionContinuity\_t} on the
  \texttt{FlowSolution\_t} node to indicate whether the field is
  continuous or discontinuous.
\end{enumerate}

For lower-order solutions using \texttt{GridLocation\_t = Vertex} or
\texttt{CellCenter}, this CPEX can be used independently of CPEX~0045.
See Section~\ref{sec:shared-vertex-layout} for the prescribed data
layout when DG DOFs coincide with shared mesh vertices.

%% ═════════════════════════════════════════════════════════════════════════
\section{Backward Compatibility}
%% ═════════════════════════════════════════════════════════════════════════

\begin{itemize}[leftmargin=2em]
\item \textbf{Existing files:} Fully compatible.  The
  \texttt{SolutionContinuity} node is optional; its absence is
  equivalent to \texttt{SolutionContinuityNull}.  No existing file
  is invalidated.

\item \textbf{Existing readers:} Unaffected.  Per CGNS convention,
  readers silently ignore unknown child nodes.  An older library
  reading a file with \texttt{SolutionContinuity} nodes will
  simply skip them.

\item \textbf{Existing writers:} Unaffected.  Codes that do not call
  the new API functions produce files without
  \texttt{SolutionContinuity} nodes, identical to current
  behavior.

\item \textbf{ABI:} The \texttt{cgns\_sol}, \texttt{cgns\_discrete},
  and \texttt{cgns\_subreg} structures each grow by two fields (one
  \texttt{int}-sized enum and one \texttt{double} node identifier).
  This is an ABI change requiring recompilation of code that directly
  accesses the internal structures.  The public API is purely additive
  (new functions, new enum), so no existing function signatures change.

\item \textbf{Parallel I/O (\texttt{cgp\_*}):} The
  \texttt{SolutionContinuity} metadata node is a small scalar
  child of \texttt{FlowSolution\_t} and carries no per-element data.
  It is written and read via the standard collective-write /
  independent-read model, identical to how \texttt{GridLocation} is
  handled in the parallel MLL.  No new \texttt{cgp\_*} variants are
  required.
\end{itemize}

%% ═════════════════════════════════════════════════════════════════════════
\section{Examples}
%% ═════════════════════════════════════════════════════════════════════════

\subsection{Example 1: Writing a Continuous Galerkin Solution (C)}
\label{ex:cg-write}

\begin{lstlisting}[language=C]
/* Create a vertex-located solution node */
int S;
cg_sol_write(fn, B, Z, "FlowSolution", CGNS_ENUMV(Vertex), &S);

/* Mark as continuous (CG FEM) */
cg_sol_continuity_write(fn, B, Z, S,
    CGNS_ENUMV(SolutionContinuous));

/* Write solution data (one value per mesh vertex, shared) */
cg_field_write(fn, B, Z, S, CGNS_ENUMV(RealDouble),
    "Pressure", pressure, &F);
\end{lstlisting}

\subsection{Example 2: Writing a Discontinuous Galerkin Solution (C)}
\label{ex:dg-write}

\begin{lstlisting}[language=C]
/* Create an interpolation-point-located solution node */
int S;
cg_sol_write(fn, B, Z, "FlowSolution_DG",
    CGNS_ENUMV(InterpolationPoints), &S);

/* Set polynomial order (CPEX 0045) */
cg_sol_interpolation_order_write(fn, B, Z, S, 3, 0);

/* Mark as discontinuous */
cg_sol_continuity_write(fn, B, Z, S,
    CGNS_ENUMV(SolutionDiscontinuous));

/* Write element-local DOFs */
cg_field_write(fn, B, Z, S, CGNS_ENUMV(RealDouble),
    "Pressure", pressure_dg, &F);
\end{lstlisting}

\subsection{Example 3: Reading and Dispatching on Continuity (C)}
\label{ex:dispatch}

\begin{lstlisting}[language=C]
CGNS_ENUMT(SolutionContinuity_t) repr;
cg_sol_continuity_read(fn, B, Z, S, &repr);

switch (repr) {
case CGNS_ENUMV(SolutionContinuous):
    /* Shared DOFs: interpolate using global mesh connectivity */
    visualize_continuous(fn, B, Z, S);
    break;
case CGNS_ENUMV(SolutionDiscontinuous):
    /* Element-local DOFs: render per-element, preserve jumps */
    visualize_discontinuous(fn, B, Z, S);
    break;
default:
    /* Null or UserDefined: fall back to legacy heuristics */
    visualize_legacy(fn, B, Z, S);
    break;
}
\end{lstlisting}

\subsection{Example 4: HDG Solver with Both Continuity Types (C)}
\label{ex:hdg}

\begin{lstlisting}[language=C]
/* Volume DOFs (discontinuous) */
int S_vol;
cg_sol_write(fn, B, Z, "VolumeSolution",
    CGNS_ENUMV(InterpolationPoints), &S_vol);
cg_sol_continuity_write(fn, B, Z, S_vol,
    CGNS_ENUMV(SolutionDiscontinuous));

/* Trace DOFs on element faces (continuous on skeleton).
 * Case A: p=0 traces (one DOF per face) -- use FaceCenter. */
int S_trace;
cg_sol_write(fn, B, Z, "TraceSolution",
    CGNS_ENUMV(FaceCenter), &S_trace);
cg_sol_continuity_write(fn, B, Z, S_trace,
    CGNS_ENUMV(SolutionContinuous));

/* Case B: higher-order traces -- use InterpolationPoints
 * with CPEX 0045 metadata for the face polynomial basis. */
int S_trace_ho;
cg_sol_write(fn, B, Z, "TraceSolution_HO",
    CGNS_ENUMV(InterpolationPoints), &S_trace_ho);
cg_sol_interpolation_order_write(fn, B, Z, S_trace_ho, 2, 0);
cg_sol_continuity_write(fn, B, Z, S_trace_ho,
    CGNS_ENUMV(SolutionContinuous));
\end{lstlisting}

\subsection{Example 5: Fortran Usage}
\label{ex:fortran}

\begin{lstlisting}[language=Fortran]
integer :: S, ier, repr

! Write a continuous solution
call cg_sol_write_f(fn, B, Z, 'FlowSolution', Vertex, S, ier)
call cg_sol_continuity_write_f(fn, B, Z, S, &
    SolutionContinuous, ier)

! Read continuity
call cg_sol_continuity_read_f(fn, B, Z, S, repr, ier)
if (repr == SolutionDiscontinuous) then
    ! Element-local interpolation
end if
\end{lstlisting}

\subsection{Example 6: Low-Order Finite Volume (DG $p=0$)}
\label{ex:fv}

\begin{lstlisting}[language=C]
/* Cell-centered FV solution */
int S;
cg_sol_write(fn, B, Z, "FlowSolution",
    CGNS_ENUMV(CellCenter), &S);

/* Explicitly mark as discontinuous */
cg_sol_continuity_write(fn, B, Z, S,
    CGNS_ENUMV(SolutionDiscontinuous));
\end{lstlisting}

This example demonstrates that the continuity metadata is useful
even for traditional low-order methods.  While a \texttt{CellCenter}
solution is inherently element-local, explicitly recording the
continuity aids post-processors and coupling frameworks that need
to distinguish between solution types programmatically.

\subsection{Example 7: DG Solution on a Shared-Vertex Mesh (C)}
\label{ex:shared-vertex-dg}

This example addresses the case where the mesh uses shared vertices
(enabling stream tracing and mesh walking via standard
\texttt{Elements\_t} connectivity), but each element stores its own
independent solution values at those vertices.  The
\texttt{SolutionContinuity\_t} node is the flag that tells readers
the repeated vertex indices in the \texttt{PointList} are independent
DOFs, not duplicates.

\begin{lstlisting}[language=C]
/*
 * Mesh: nVerts shared vertices, nElems elements,
 *       nVertsPerElem vertices per element.
 * Solution: element-local DOFs at each element's vertices.
 *           Total DOFs = nElems * nVertsPerElem.
 */

/* Build the point list: for each element, list its vertex indices.
 * Shared vertices appear multiple times -- once per adjacent element. */
cgsize_t point_list[nElems * nVertsPerElem];
for (int e = 0; e < nElems; e++)
    for (int v = 0; v < nVertsPerElem; v++)
        point_list[e * nVertsPerElem + v] =
            element_connectivity[e][v];  /* 1-based vertex index */

/* Create solution node with PointList using the idiomatic
 * cg_sol_ptset_write: Vertex location preserves shared-mesh
 * connectivity for geometry operations.
 * DataSize = nElems * nVertsPerElem (not nVerts). */
int S;
cg_sol_ptset_write(fn, B, Z, "FlowSolution_DG",
    CGNS_ENUMV(Vertex),
    CGNS_ENUMV(PointList),
    (cgsize_t)(nElems * nVertsPerElem),
    point_list, &S);

/* Mark as discontinuous: values at repeated vertex indices
 * are independent across elements, ordered by Elements_t
 * section index then connectivity order. */
cg_sol_continuity_write(fn, B, Z, S,
    CGNS_ENUMV(SolutionDiscontinuous));

/* Write element-local solution data */
cg_field_write(fn, B, Z, S, CGNS_ENUMV(RealDouble),
    "Pressure", pressure_dg, &F);
\end{lstlisting}

A reader distinguishes this from a continuous solution as follows:

\begin{lstlisting}[language=C]
CGNS_ENUMT(SolutionContinuity_t) cont;
cg_sol_continuity_read(fn, B, Z, S, &cont);

if (cont == CGNS_ENUMV(SolutionDiscontinuous)) {
    /* PointList entries are element-local DOF indices.
     * Values at the same vertex index from different elements
     * are independent -- do not average. */
}
\end{lstlisting}

\subsection{Example 8: DiscreteData\_t with Discontinuous Error Indicator (C)}
\label{ex:discrete}

This example shows a DG error indicator stored in a
\texttt{DiscreteData\_t} node.  The usage mirrors
\texttt{FlowSolution\_t}; the only difference is the API prefix
(\texttt{cg\_discrete\_} instead of \texttt{cg\_sol\_}).

\begin{lstlisting}[language=C]
/* Create a cell-centered discrete data node for error indicators */
int D;
cg_discrete_write(fn, B, Z, "ErrorIndicator", &D);
cg_goto(fn, B, "Zone_t", Z, "DiscreteData_t", D, "end");
cg_gridlocation_write(CGNS_ENUMV(CellCenter));

/* Mark as discontinuous (element-local error estimates) */
cg_discrete_continuity_write(fn, B, Z, D,
    CGNS_ENUMV(SolutionDiscontinuous));

/* Write per-element error data */
cg_goto(fn, B, "Zone_t", Z, "DiscreteData_t", D, "end");
cg_array_write("ElementError", CGNS_ENUMV(RealDouble),
    1, &nElems, error_data);
\end{lstlisting}

\subsection{Example 9: ZoneSubRegion\_t with Boundary Flux Data (C)}
\label{ex:subreg}

This example stores discontinuous boundary flux data on a
\texttt{ZoneSubRegion\_t} that references a boundary condition
region.

\begin{lstlisting}[language=C]
/* Create a sub-region referencing a BC */
int SR;
/* CellDimension is the zone's cell dimension (e.g., 3 for a 3-D volume
 * zone), so CellDimension - 1 is the face dimension. */
cg_subreg_bcname_write(fn, B, Z, "WallFluxes",
    CellDimension - 1, "Wall_BC", &SR);

/* Mark as discontinuous (each face owns its own flux values) */
cg_subreg_continuity_write(fn, B, Z, SR,
    CGNS_ENUMV(SolutionDiscontinuous));

/* Write flux data on the sub-region */
cg_goto(fn, B, "Zone_t", Z, "ZoneSubRegion_t", SR, "end");
cg_array_write("HeatFlux", CGNS_ENUMV(RealDouble),
    1, &nBCFaces, heat_flux);
\end{lstlisting}

\subsection{Example 10: Solver Restart with Continuity-Aware Read (C)}
\label{ex:restart}

A solver reading a restart file must reconstruct the solution state
correctly based on the continuity.  This example shows how a restart
reader uses the continuity metadata to dispatch between shared-DOF
and element-local reconstruction.

\begin{lstlisting}[language=C]
/* Read solution metadata */
char sol_name[CGIO_MAX_NAME_LENGTH+1];
CGNS_ENUMT(GridLocation_t) loc;
cg_sol_info(fn, B, Z, S, sol_name, &loc);

CGNS_ENUMT(SolutionContinuity_t) cont;
cg_sol_continuity_read(fn, B, Z, S, &cont);

/* Read the field data.  In a generic restart loop, call
 * cg_field_info(fn,B,Z,S,F,...) first to discover field names
 * and types before calling cg_field_read. */
cg_field_read(fn, B, Z, S, "Pressure",
    CGNS_ENUMV(RealDouble), range_min, range_max,
    pressure_buf);

/* Reconstruct based on continuity */
if (cont == CGNS_ENUMV(SolutionContinuous)) {
    /* DOFs are shared at mesh vertices/edges/faces.
     * Map directly to the global DOF vector. */
    reconstruct_continuous(pressure_buf, loc, mesh);
} else if (cont == CGNS_ENUMV(SolutionDiscontinuous)) {
    /* DOFs are element-local.  Each element owns its
     * own copy of values at shared interfaces. */
    reconstruct_discontinuous(pressure_buf, loc, mesh);
} else {
    /* Null: legacy file.  Infer from data size or
     * assume continuous for backward compatibility. */
    reconstruct_legacy(pressure_buf, loc, mesh);
}
\end{lstlisting}

\subsection{Example 11: FSI Coupling --- Structural and Fluid Solutions (C)}
\label{ex:fsi}

In Fluid-Structure Interaction (FSI) applications, a single zone may
carry both fluid and structural solution fields with different
continuity types.  A continuous FEM structural solver stores shared
displacement DOFs, while a DG fluid solver stores element-local flow
variables.  Separate \texttt{FlowSolution\_t} nodes with distinct
continuity metadata allow coupling frameworks to correctly interpret
and transfer each field.

\begin{lstlisting}[language=C]
/* Structural solution: continuous FEM displacements */
int S_struct;
cg_sol_write(fn, B, Z, "StructuralDisplacement",
    CGNS_ENUMV(Vertex), &S_struct);
cg_sol_continuity_write(fn, B, Z, S_struct,
    CGNS_ENUMV(SolutionContinuous));
cg_field_write(fn, B, Z, S_struct, CGNS_ENUMV(RealDouble),
    "DisplacementX", disp_x, &F);
cg_field_write(fn, B, Z, S_struct, CGNS_ENUMV(RealDouble),
    "DisplacementY", disp_y, &F);

/* Fluid solution: discontinuous DG flow field */
int S_fluid;
cg_sol_write(fn, B, Z, "FluidSolution",
    CGNS_ENUMV(InterpolationPoints), &S_fluid);
cg_sol_interpolation_order_write(fn, B, Z, S_fluid, 3, 0);
cg_sol_continuity_write(fn, B, Z, S_fluid,
    CGNS_ENUMV(SolutionDiscontinuous));
cg_field_write(fn, B, Z, S_fluid, CGNS_ENUMV(RealDouble),
    "Pressure", pressure, &F);
cg_field_write(fn, B, Z, S_fluid, CGNS_ENUMV(RealDouble),
    "VelocityX", vel_x, &F);

/* A coupling framework reading this zone can query
 * each FlowSolution_t's continuity to determine the
 * correct interpolation and transfer strategy:
 *   - Continuous fields: global shared-DOF interpolation
 *   - Discontinuous fields: element-local projection */
\end{lstlisting}

The same pattern applies when the structural and fluid domains are in
separate zones.  The continuity metadata on each zone's
\texttt{FlowSolution\_t} nodes enables the coupling framework to
apply the correct inter-code transfer operators without relying on
naming conventions or external configuration.

\subsection{Example 12: Conjugate Heat Transfer --- Perfect and Imperfect Thermal Contact (C)}
\label{ex:cht}

Conjugate heat transfer (CHT) simulations couple a fluid temperature
field (often stored at cell centers or DG DOFs) with a solid
temperature field (often stored as a continuous CG FEM solution).
The continuity metadata disambiguates the two layouts and informs the
coupling framework how to perform the interface transfer.

\begin{lstlisting}[language=C]
/* ---- Fluid zone: FV/DG temperature (discontinuous) ---- */
int S_fluid;
cg_sol_write(fn, B, fluidZone, "ThermalSolution",
    CGNS_ENUMV(CellCenter), &S_fluid);
cg_sol_continuity_write(fn, B, fluidZone, S_fluid,
    CGNS_ENUMV(SolutionDiscontinuous));
cg_field_write(fn, B, fluidZone, S_fluid,
    CGNS_ENUMV(RealDouble), "Temperature", T_fluid, &F);

/* ---- Solid zone: CG FEM temperature (continuous) ---- */
int S_solid;
cg_sol_write(fn, B, solidZone, "ThermalSolution",
    CGNS_ENUMV(Vertex), &S_solid);
cg_sol_continuity_write(fn, B, solidZone, S_solid,
    CGNS_ENUMV(SolutionContinuous));
cg_field_write(fn, B, solidZone, S_solid,
    CGNS_ENUMV(RealDouble), "Temperature", T_solid, &F);

/* ---- Interface heat flux on the solid side (face-local) ---- */
int SR;
cg_subreg_bcname_write(fn, B, solidZone, "FluidInterface",
    CellDimension - 1, "FluidInterface_BC", &SR);
cg_subreg_continuity_write(fn, B, solidZone, SR,
    CGNS_ENUMV(SolutionDiscontinuous));
cg_goto(fn, B, "Zone_t", solidZone,
    "ZoneSubRegion_t", SR, "end");
cg_array_write("HeatFlux", CGNS_ENUMV(RealDouble),
    1, &nInterfaceFaces, heat_flux);
\end{lstlisting}

With \emph{perfect} thermal contact the fluid-side and solid-side
interface temperatures are equal; the coupling framework can enforce
this constraint knowing the solid temperature is
\texttt{SolutionContinuous} (shared vertex DOFs) and the fluid
temperature is \texttt{SolutionDiscontinuous} (cell-centered).

With \emph{imperfect} thermal contact (finite thermal resistance),
a temperature jump $\Delta T$ exists at the interface.  Each domain
retains its own interface temperature, and the coupling framework
applies the resistance relation $q = \Delta T / R$ between them.
No change to the \texttt{SolutionContinuity\_t} labels is needed:
the fluid and solid temperatures are stored and tagged independently
in their respective zones, and the jump is an emergent result of the
coupling rather than a property of either field's storage layout.

%% ═════════════════════════════════════════════════════════════════════════
\section{SIDS Impact}
%% ═════════════════════════════════════════════════════════════════════════

\begin{enumerate}[leftmargin=2em]
\item \textbf{New enumeration type:} \texttt{SolutionContinuity\_t}
  is added to the SIDS building-block structure definitions
  (Section~4 of the SIDS document), alongside existing enumeration
  types such as \texttt{GridLocation\_t} (Section~4.5) and
  \texttt{DataClass\_t} (Section~4.1).

\item \textbf{FlowSolution\_t amendment:} The
  \texttt{FlowSolution\_t} node definition (SIDS Section~7.7) is
  amended to include an optional
  \texttt{SolutionContinuity\_t} child node.

\item \textbf{DiscreteData\_t amendment:} The
  \texttt{DiscreteData\_t} node definition (SIDS Section~12.4) is
  similarly amended.

\item \textbf{ZoneSubRegion\_t amendment:} The
  \texttt{ZoneSubRegion\_t} node definition (SIDS Section~7.9) is
  similarly amended.

\item \textbf{PointList semantics amendment:} The description of
  \texttt{PointList} under \texttt{FlowSolution\_t} in SIDS~§7.7 is
  amended to permit repeated vertex indices when
  \texttt{SolutionContinuity\_t = SolutionDiscontinuous}.  Repeated
  indices in that context represent element-local DOFs and are not
  malformed duplicates; the DOF-to-element mapping follows ascending
  \texttt{Elements\_t} section index order, then connectivity order
  within each section.  This amendment does not affect \texttt{PointList}
  semantics under any other condition.

\item No existing SIDS node types, enumerations, or conventions are
  modified or removed.
\end{enumerate}

%% ═════════════════════════════════════════════════════════════════════════
\section{File Mapping Impact}
%% ═════════════════════════════════════════════════════════════════════════

The \texttt{SolutionContinuity} child node is stored identically
to \texttt{GridLocation}: as a character node containing the string
name of the enumeration value.  The file-mapping attributes are:

\begin{center}
\begin{tabular}{@{} l l @{}}
\toprule
\textbf{Attribute} & \textbf{Value} \\
\midrule
Node name  & \texttt{SolutionContinuity} \\
Label      & \texttt{SolutionContinuity\_t} \\
Data type  & \texttt{C1} (character) \\
Dimension  & 1 \\
Dimension value & length of enumeration string \\
Data       & enumeration name string \\
\bottomrule
\end{tabular}
\end{center}

\noindent This follows the same pattern as the \texttt{GridLocation}
node under \texttt{FlowSolution\_t}: node name
\texttt{"GridLocation"}, label \texttt{"GridLocation\_t"}, data type
\texttt{"C1"}, with the enumeration string as data.

The node is written only when the continuity is not
\texttt{SolutionContinuityNull}.  When absent, readers default to
\texttt{SolutionContinuityNull}, paralleling how an absent
\texttt{GridLocation} node defaults to \texttt{Vertex}.

%% ═════════════════════════════════════════════════════════════════════════
\section{Design Decisions}
%% ═════════════════════════════════════════════════════════════════════════

The following design questions were considered during the development
of this proposal.  Each is resolved below with rationale grounded in
existing CGNS conventions and SIDS precedent.

\begin{enumerate}[leftmargin=2em]

\item \textbf{Scope: \texttt{BCData\_t} is excluded.}
  \texttt{SolutionContinuity\_t} is \emph{not} added to
  \texttt{BCData\_t}.  Boundary condition data arrays in
  \texttt{BCData\_t} are sized by the associated
  \texttt{PointList}/\texttt{PointRange} and \texttt{GridLocation\_t},
  which already determine the data layout unambiguously.  In DG
  methods, boundary conditions are typically applied weakly through
  numerical fluxes; the element-local nature of the boundary data
  follows from the solution continuity on the parent
  \texttt{FlowSolution\_t} node.  Adding
  \texttt{SolutionContinuity\_t} to \texttt{BCData\_t} would be
  redundant and would introduce a potential inconsistency if the BC
  continuity differs from the solution it constrains.  Should a
  future use case require per-BC continuity metadata (e.g.,
  coupling frameworks imposing boundary data in a different
  continuity than the interior, or interface conditions in CHT
  simulations with imperfect thermal contact where a flux-jump
  boundary condition is prescribed), it can be proposed as a
  separate, narrowly scoped CPEX without affecting this proposal.
  The \texttt{ZoneSubRegion\_t} extension introduced here
  partially addresses such cases: interface fluxes or
  surface-local fields that require a continuity label can
  be stored in a \texttt{ZoneSubRegion\_t} referencing the
  relevant boundary condition (see Example~\ref{ex:cht}).

\item \textbf{Per-solution-node granularity (not per-field).}
  The continuity applies at the \texttt{FlowSolution\_t} level,
  meaning all \texttt{DataArray\_t} children share the same
  continuity.  This follows the precedent set by
  \texttt{GridLocation\_t}, which is likewise a per-solution-node
  property: all fields within a \texttt{FlowSolution\_t} node share
  the same grid location.  Per-\texttt{DataArray\_t} granularity was
  rejected because it would add significant API and metadata
  complexity with no demonstrated need.  Solvers that genuinely mix
  continuity types within a single zone (e.g., HDG methods with
  continuous traces and discontinuous volumes) should use separate
  \texttt{FlowSolution\_t} nodes, exactly as they would use separate
  nodes for fields at different \texttt{GridLocation\_t} values.

\item \textbf{\texttt{CellCenter} + \texttt{SolutionContinuous}: permitted
  but semantically inconsistent.}
  The MLL does \emph{not} enforce consistency between
  \texttt{SolutionContinuity\_t} and \texttt{GridLocation\_t}.
  A \texttt{CellCenter} solution is inherently element-local, making
  \texttt{SolutionContinuous} semantically contradictory, but the library
  shall not reject such combinations.  This follows the established
  CGNS convention of being permissive in what is accepted: the
  standard documents correct usage; validation is delegated to
  application-level tools such as \texttt{cgnscheck}.

  \textbf{Convention:} \texttt{SolutionContinuity\_t = SolutionContinuous}
  with \texttt{GridLocation\_t = CellCenter} should not be used.
  \texttt{cgnscheck} should issue a warning when this combination is
  encountered.

\item \textbf{Recommended \texttt{cgnscheck} validation rules.}
  The following consistency checks are recommended for
  \texttt{cgnscheck} and similar validation tools:

  \begin{description}[leftmargin=2em]
  \item[Warning:] \texttt{SolutionContinuity\_t = SolutionContinuous} with
    \texttt{GridLocation\_t = CellCenter} --- semantically inconsistent
    since cell-centered data is inherently element-local
    (Design Decision~3 above).

  \item[Error:] \texttt{SolutionContinuity\_t = SolutionDiscontinuous} with
    \texttt{GridLocation\_t = Vertex} and \emph{no}
    \texttt{PointList} --- the SIDS \texttt{DataSize} formula yields
    $\texttt{VertexSize}$ (the shared-vertex count), which cannot
    represent the larger element-local DOF count
    (Section~\ref{sec:shared-vertex-layout}).

  \item[Warning:] \texttt{SolutionContinuity\_t} value outside the
    range $[0, \texttt{NofValidSolutionContinuity})$ --- invalid
    enumeration value.
  \end{description}

\end{enumerate}

%% ═════════════════════════════════════════════════════════════════════════
\section{Summary of API Additions}
%% ═════════════════════════════════════════════════════════════════════════

\begin{center}
\begin{longtable}{l p{8.5cm}}
\toprule
\textbf{Symbol} & \textbf{Description} \\
\midrule
\endhead
\texttt{SolutionContinuity\_t} & New enumeration type: \texttt{Null}, \texttt{UserDefined}, \texttt{SolutionContinuous}, \texttt{SolutionDiscontinuous}. \\
\texttt{SolutionContinuityName} & String table for the enumeration values. \\
\texttt{NofValidSolutionContinuity} & Count of valid enumeration values (4). \\
\texttt{cg\_SolutionContinuityName} & C: return the string name for a \texttt{SolutionContinuity\_t} value (mirrors \texttt{cg\_GridLocationName}). \\
\texttt{cg\_sol\_continuity\_write} & C: write solution continuity for a \texttt{FlowSolution\_t} node. \\
\texttt{cg\_sol\_continuity\_read} & C: read solution continuity for a \texttt{FlowSolution\_t} node. \\
\texttt{cg\_discrete\_continuity\_write} & C: write solution continuity for a \texttt{DiscreteData\_t} node. \\
\texttt{cg\_discrete\_continuity\_read} & C: read solution continuity for a \texttt{DiscreteData\_t} node. \\
\texttt{cg\_subreg\_continuity\_write} & C: write solution continuity for a \texttt{ZoneSubRegion\_t} node. \\
\texttt{cg\_subreg\_continuity\_read} & C: read solution continuity for a \texttt{ZoneSubRegion\_t} node. \\
\texttt{cg\_sol\_continuity\_write\_f} & Fortran: write solution continuity (\texttt{FlowSolution\_t}). \\
\texttt{cg\_sol\_continuity\_read\_f} & Fortran: read solution continuity (\texttt{FlowSolution\_t}). \\
\texttt{cg\_discrete\_continuity\_write\_f} & Fortran: write solution continuity (\texttt{DiscreteData\_t}). \\
\texttt{cg\_discrete\_continuity\_read\_f} & Fortran: read solution continuity (\texttt{DiscreteData\_t}). \\
\texttt{cg\_subreg\_continuity\_write\_f} & Fortran: write solution continuity (\texttt{ZoneSubRegion\_t}). \\
\texttt{cg\_subreg\_continuity\_read\_f} & Fortran: read solution continuity (\texttt{ZoneSubRegion\_t}). \\
\bottomrule
\end{longtable}
\end{center}

%% ═════════════════════════════════════════════════════════════════════════
\section{References}
%% ═════════════════════════════════════════════════════════════════════════

\begin{enumerate}[leftmargin=2em]
\item CGNS Standard Interface Data Structures (SIDS),\\
  \url{https://cgns.org/standard/SIDS/CGNS_SIDS.html}

\item CGNS Mid-Level Library---Flow Solution,\\
  \url{https://cgns.org/standard/MLL/api/c_api.html}

\item CPEX 0045, ``Polynomial Data and Curved Grid Elements,''\\
  \url{https://github.com/CGNS/CGNS/issues/577}

\item Hesthaven, J.S.\ and Warburton, T., \emph{Nodal Discontinuous
  Galerkin Methods}, Springer, 2008.

\item Cockburn, B., Karniadakis, G.E., and Shu, C.-W., eds.,
  \emph{Discontinuous Galerkin Methods: Theory, Computation, and
  Applications}, Springer, 2000.
\end{enumerate}

\end{document}
